# --- Runtime ---
fastapi>=0.111,<0.116
uvicorn[standard]>=0.30,<0.35
sqlalchemy[asyncio]>=2.0.30,<2.1
asyncpg>=0.29,<0.31
alembic>=1.13,<1.15
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
PyJWT[crypto]>=2.8,<3.0
structlog>=26.1.0
sentry-sdk[fastapi]>=2.0

# --- Tooling / tests (kept here so CI installs them with one file) ---
httpx>=0.27
aiosqlite>=0.20
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
# Upper-bounded on purpose: `ruff format --check` runs in CI, and formatter output
# can shift between minor versions. An unbounded pin would fail an unrelated PR the
# day a new ruff ships. Keep in sync with the rev in .pre-commit-config.yaml.
ruff>=0.15,<0.16
mypy>=1.10
